/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'via.placeholder.com',
      'cms.tamojunto.org.br',
      'prod-cms-us-east-1-uploads.s3.us-east-1.amazonaws.com',
    ],
  },
};

export default nextConfig;
